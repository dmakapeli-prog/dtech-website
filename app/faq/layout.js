export const metadata = {
  title: "FAQ | DiCode",
  description: "Pertanyaan yang sering ditanyakan tentang layanan DiCode."
};

export default function FAQLayout({ children }) {
  return <>{children}</>;
}
