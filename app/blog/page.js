import BlogClient from "./BlogClient";

export const metadata = {
  title: "Blog | DiCode",
  description: "Tips, tutorial, dan insight seputar website dan undangan digital dari tim DiCode.",
};

export default function BlogPage() {
  return <BlogClient />;
}
