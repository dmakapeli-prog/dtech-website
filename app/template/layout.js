export const metadata = {
  title: "Template & Katalog | DiCode",
  description: "Pilih dari 10+ template website profesional DiCode. Template kuliner, e-commerce, company profile, undangan digital, dan lainnya."
};

export default function TemplateLayout({ children }) {
  return <>{children}</>;
}
