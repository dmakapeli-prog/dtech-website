export const metadata = {
  title: "Portofolio | DiCode",
  description: "Hasil karya dan project DiCode dalam pembuatan website dan undangan digital."
};

export default function PortofolioLayout({ children }) {
  return <>{children}</>;
}
